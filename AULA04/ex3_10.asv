close all; clear all; clc;
manIMG = imread('cameraman.tif');
phi = 30;
phi_rad = pi*phi/180;

A = [cos(phi_rad) sin(phi_rad) 0, -sin(phi_rad) cos(phi_rad) 0, 0 0 1];

[img_height, img_length] = size(manIMG);
manIMG1 = [manIMG ones(img_height,1)];

for u=1:1:img_heght
    for v=1:1:img_legth
    [x,v]
    end
end